import { OctagonAlert,ShieldCheck } from 'lucide-react';import type { RiskLevel } from '../../types/risk';import { Card } from '../common/StatusBadge'
export function RecommendedAction({decision,recommendation,level}:{decision:string|null;recommendation?:string|null;level:RiskLevel}){const critical=level==='CRITICAL';return <Card className={critical?'border-red-500/50 bg-red-500/5':''}><div className="flex gap-4">{critical?<OctagonAlert className="h-7 w-7 shrink-0 text-red-400"/>:<ShieldCheck className="h-7 w-7 shrink-0 text-cyan"/>}<div><p className="metric-label">Recommended security action</p><h3 className={`mt-2 text-lg font-bold ${critical?'text-red-200':'text-white'}`}>{decision??'Awaiting backend decision'}</h3><p className="mt-1 text-sm text-slate-300">{recommendation??'No recommendation has been received from the risk engine.'}</p>{critical&&<p className="mt-3 text-sm font-bold uppercase tracking-wider text-red-300">Do not authorize sensitive action</p>}</div></div></Card>}

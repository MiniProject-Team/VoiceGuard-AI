import type { Session } from '../../types/session';import { duration,shortId } from '../../utils/formatting';import { Card,StatusBadge } from '../common/StatusBadge'
export function CallStatusCard({session}:{session:Session}){return <Card><div className="flex items-center justify-between"><p className="metric-label">Call status</p><StatusBadge label={session.status}/></div><dl className="mt-4 grid grid-cols-3 gap-3 text-sm"><div><dt className="text-xs text-slate-500">Session</dt><dd className="mt-1 font-mono text-slate-200" title={session.session_id}>{shortId(session.session_id)}</dd></div><div><dt className="text-xs text-slate-500">Duration</dt><dd className="mt-1 text-slate-200">{duration(session.started_at,session.completed_at)}</dd></div><div><dt className="text-xs text-slate-500">Segments</dt><dd className="mt-1 text-slate-200">{session.segments_processed}</dd></div></dl></Card>}

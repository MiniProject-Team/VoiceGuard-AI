import { AudioWaveform } from 'lucide-react';import { percent } from '../../utils/formatting';import { Card } from '../common/StatusBadge'
export function SyntheticVoiceCard({value}:{value:number|null}){const assessment=value==null?'Awaiting analysis':value>=.75?'High probability of synthetic audio':value>=.5?'Synthetic indicators detected':'Low synthetic probability';return <Card><div className="flex justify-between"><p className="metric-label">AI voice detection</p><AudioWaveform className="h-5 w-5 text-cyan"/></div><p className="mt-5 text-4xl font-semibold text-white">{percent(value)}</p><p className="mt-2 text-xs font-semibold uppercase tracking-wider text-slate-400">{assessment}</p></Card>}

import type { RiskLevel } from '../../types/risk';import { getRiskColor } from '../../utils/risk';import { Card,StatusBadge } from '../common/StatusBadge'
export function RiskScoreCard({score,level}:{score:number|null;level:RiskLevel}){const value=score??0;return <Card><p className="metric-label">Risk score</p><div className="mt-4 flex items-end justify-between"><div><span className="text-4xl font-semibold tabular-nums text-white">{score==null?'—':Math.round(score)}</span><span className="ml-1 text-sm text-slate-500">/ 100</span></div><StatusBadge label={level} tone={level}/></div><div className="mt-5 h-2 overflow-hidden rounded-full bg-slate-800" role="meter" aria-label="Risk score" aria-valuemin={0} aria-valuemax={100} aria-valuenow={score??undefined}><div className="h-full rounded-full transition-all duration-500" style={{width:`${value}%`,backgroundColor:getRiskColor(level)}}/></div></Card>}

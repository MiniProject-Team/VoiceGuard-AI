import { CartesianGrid,Line,LineChart,ReferenceLine,ResponsiveContainer,Tooltip,XAxis,YAxis } from 'recharts';import type { SegmentResult } from '../../types/risk';import { Card } from '../common/StatusBadge';import { EmptyState } from '../common/EmptyState'
export function RiskChart({results}:{results:SegmentResult[]}){if(!results.length)return <Card className="min-h-80"><p className="section-title">Risk timeline</p><EmptyState title="No risk samples" message="The chart updates after the backend analyzes an audio segment."/></Card>;return <Card className="min-h-80"><p className="section-title">Risk timeline</p><div className="mt-5 h-60" aria-label="Risk score by segment"><ResponsiveContainer width="100%" height="100%"><LineChart data={results}><CartesianGrid stroke="#202c3e" strokeDasharray="3 3"/><XAxis dataKey="segment_id" stroke="#64748b" tickLine={false}/><YAxis domain={[0,100]} stroke="#64748b" tickLine={false}/><Tooltip contentStyle={{background:'#0d1422',border:'1px solid #202c3e',borderRadius:8}}/><ReferenceLine y={75} stroke="#f87171" strokeDasharray="4 4"/><Line type="monotone" dataKey="risk_score" stroke="#25c2e8" strokeWidth={2} dot={{fill:'#25c2e8',r:3}} isAnimationActive={false}/></LineChart></ResponsiveContainer></div></Card>}

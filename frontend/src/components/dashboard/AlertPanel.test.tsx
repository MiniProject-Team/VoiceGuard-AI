import { render,screen } from '@testing-library/react';import { describe,expect,it } from 'vitest';import { AlertPanel } from './AlertPanel'
describe('AlertPanel',()=>{it('keeps recommendation and severity visible',()=>{render(<AlertPanel alerts={[{severity:'critical',alert_type:'voice_impersonation',message:'Potential impersonation detected.',recommended_action:'Verify independently.'}]}/>);expect(screen.getByText('critical')).toBeInTheDocument();expect(screen.getByText(/Verify independently/)).toBeInTheDocument()});it('shows an honest empty state',()=>{render(<AlertPanel alerts={[]}/>);expect(screen.getByText('No security alerts')).toBeInTheDocument()})})

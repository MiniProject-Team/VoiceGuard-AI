import { Fingerprint } from 'lucide-react';import { percent } from '../../utils/formatting';import { Card } from '../common/StatusBadge'
export function SpeakerVerificationCard({value,status,verified}:{value:number|null;status?:string;verified?:boolean|null}){const text=status==='speaker_not_enrolled'?'No enrollment for claimed ID':status==='threshold_not_calibrated'?'Threshold not calibrated':status==='unavailable'?'Verification unavailable':value==null?'Awaiting verification':verified===true?'Reference match':verified===false?'Identity mismatch':'Verification pending';return <Card><div className="flex justify-between"><p className="metric-label">Speaker verification</p><Fingerprint className="h-5 w-5 text-blue-400"/></div><p className="mt-5 text-4xl font-semibold text-white">{percent(value)}</p><p className="mt-2 text-xs font-semibold uppercase tracking-wider text-slate-400">{text}</p></Card>}

# Demo guide
Validate, start `python scripts/start_sih_demo.py`, run `npm run dev` in `frontend`, and open System Status before Live Analysis. Use `demo/scenario_1_genuine` through `scenario_4_clone_transaction`. Report actual outputs: the latest real run produced MEDIUM, MEDIUM, MEDIUM, and HIGH, so do not claim the current model meets the conceptual scenario targets.

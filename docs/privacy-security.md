# Privacy and security
Raw audio stays in memory; APIs never expose embeddings. IDs are opaque, logs redacted, configs hashed, and audits SHA-256 chained. Hardened mode restricts CORS/debug and bounds/rate-limits resources. Production requires TLS/WSS, RBAC, secrets management, encryption, segmentation, SIEM, gateway limits, and independent append-only audit storage.
